/** Tailwind is imported by `app/globals.css` for its reset; the site's own
 *  styles are hand-written custom properties, not utility classes. */
const config = { plugins: { '@tailwindcss/postcss': {} } };

export default config;
